name: Semantic Release

on:
  push:
    branches:
      - main
      - release/*

permissions: {}

jobs:
  semantic-release:
    permissions:
      contents: read
    uses: radiorabe/actions/.github/workflows/semantic-release.yaml@7a3366ebda9b91bf943bf09527f4d7a5d8d10800 # v0.44.0
    secrets:
      RABE_ITREAKTION_GITHUB_TOKEN: ${{ secrets.RABE_ITREAKTION_GITHUB_TOKEN }}
