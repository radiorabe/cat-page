name: Scheduled tasks

on:
  schedule:
    - cron:  '13 12 * * *'
  workflow_dispatch:

permissions: {}

jobs:
  schedule-trivy:
    permissions:
      packages: write
      security-events: write
      id-token: write
    uses: radiorabe/actions/.github/workflows/schedule-trivy.yaml@v0.40.0
    with:
      image-ref: 'ghcr.io/radiorabe/catpage:latest'
